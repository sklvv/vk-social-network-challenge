import { SignInForm } from "@/features/login";
import { Box } from "@chakra-ui/react";

const LoginPage = () => {
    return (
        <Box px={"4"}>
            <SignInForm />
        </Box>
    );
};

export default LoginPage;
